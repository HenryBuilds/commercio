// MySQL doesn't support named enum types like PostgreSQL
// Enum values are enforced at the application level
// These are placeholder exports for schema compatibility
export const inventoryTransactionTypeEnum = null;
export const reservationStatusEnum = null;
export const orderStatusEnum = null;
export const paymentTermsEnum = null;
